let audioContext=null;let lastImpactAt=0;function context(){if(audioContext)return audioContext;const Ctor=window.AudioContext||window.webkitAudioContext;if(!Ctor)return null;audioContext=new Ctor();return audioContext;}export function primeRtsAudio(){const ctx=context();if(ctx?.state==='suspended')ctx.resume().catch(()=>{});}export function playCombatImpact(effect){const strength=effect?.type==='sunken-destroyed'?1:effect?.type==='sunken-shot'?0.55:effect?.type==='unit-death'?0.45:effect?.type==='hydra-shot'?0.14:0;if(strength<=0)return;const now=performance.now();if(now-lastImpactAt<38)return;lastImpactAt=now;const ctx=context();if(!ctx||ctx.state!=='running')return;const osc=ctx.createOscillator();const gain=ctx.createGain();const filter=ctx.createBiquadFilter();filter.type='lowpass';filter.frequency.value=700;osc.type=effect.type==='hydra-shot'?'triangle':'sawtooth';osc.frequency.setValueAtTime(effect.type==='hydra-shot'?180:90,ctx.currentTime);osc.frequency.exponentialRampToValueAtTime(55,ctx.currentTime+0.055);gain.gain.setValueAtTime(0.0001,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(0.028*strength,ctx.currentTime+0.004);gain.gain.exponentialRampToValueAtTime(0.0001,ctx.currentTime+0.07);osc.connect(filter);filter.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+0.08);}
